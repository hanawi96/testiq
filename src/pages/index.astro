---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getLocale } from '../utils/i18n';
import LucideIcon from '../components/LucideIcon.astro';

const locale = getLocale('vi');
---

<BaseLayout
  title={locale.meta.site_name}
  description={locale.meta.description}
  keywords={locale.meta.keywords}
>
  <!-- Hero Section -->
  <section class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100 py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <!-- AI Badge -->
      <div class="text-center mb-12">
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-white/80 backdrop-blur-sm border border-blue-200 text-blue-700 text-sm font-medium shadow-sm">
          <LucideIcon name="star" size={16} class="mr-2" />
          Powered by Advanced AI
        </div>
      </div>

      <!-- Main Title -->
      <div class="text-center mb-16">
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6">
          Test <span class="bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">IQ & EQ</span> Chính Xác Nhất Thế Giới
        </h1>
        <p class="text-xl md:text-2xl text-gray-600 max-w-4xl mx-auto leading-relaxed">
          Khám phá toàn diện trí tuệ IQ và trí tuệ cảm xúc EQ với công nghệ AI tiên tiến. Kết quả tức thì, phân tích khoa học.
        </p>
      </div>

      <!-- Features Grid -->
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-6 mb-16">
        <div class="text-center p-6">
          <div class="w-16 h-16 mx-auto mb-4 bg-gradient-to-br from-blue-100 to-blue-200 rounded-2xl flex items-center justify-center">
            <LucideIcon name="check-circle" size={32} class="text-blue-600" />
          </div>
          <h3 class="font-bold text-gray-900 mb-1">Độ chính xác 99.7%</h3>
        </div>
        
        <div class="text-center p-6">
          <div class="w-16 h-16 mx-auto mb-4 bg-gradient-to-br from-green-100 to-green-200 rounded-2xl flex items-center justify-center">
            <LucideIcon name="zap" size={32} class="text-green-600" />
          </div>
          <h3 class="font-bold text-gray-900 mb-1">Kết quả tức thì</h3>
        </div>
        
        <div class="text-center p-6">
          <div class="w-16 h-16 mx-auto mb-4 bg-gradient-to-br from-purple-100 to-purple-200 rounded-2xl flex items-center justify-center">
            <LucideIcon name="bar-chart-3" size={32} class="text-purple-600" />
          </div>
          <h3 class="font-bold text-gray-900 mb-1">Phân tích khoa học</h3>
        </div>
        
        <div class="text-center p-6">
          <div class="w-16 h-16 mx-auto mb-4 bg-gradient-to-br from-orange-100 to-orange-200 rounded-2xl flex items-center justify-center">
            <LucideIcon name="award" size={32} class="text-orange-600" />
          </div>
          <h3 class="font-bold text-gray-900 mb-1">Chứng chỉ digital</h3>
        </div>
      </div>

      <!-- Test Cards -->
      <div class="grid md:grid-cols-2 gap-8 mb-16">
        <!-- IQ Test Card -->
        <div class="relative overflow-hidden rounded-3xl p-8 text-white text-center shadow-2xl hover:shadow-3xl transition-shadow duration-300">
          <!-- Blue Gradient Background -->
          <div class="absolute inset-0 bg-gradient-to-br from-blue-500 via-blue-600 to-blue-700"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 mx-auto mb-6 bg-white/20 rounded-2xl flex items-center justify-center backdrop-blur-sm">
              <LucideIcon name="brain" size={48} class="text-white" />
            </div>
            <h3 class="text-3xl font-bold mb-2">Test IQ</h3>
            <p class="text-blue-100 text-lg mb-4">Cognitive Intelligence</p>
            <div class="flex items-center justify-center text-blue-100">
              <LucideIcon name="clock" size={16} class="mr-2" />
              55 minutes • 10 questions
            </div>
            <a href="/test/iq" class="inline-block mt-6 px-8 py-3 bg-white text-blue-600 rounded-xl font-semibold hover:bg-blue-50 transition-colors">
              Bắt đầu test
            </a>
          </div>
        </div>

        <!-- EQ Test Card -->
        <div class="relative overflow-hidden rounded-3xl p-8 text-white text-center shadow-2xl hover:shadow-3xl transition-shadow duration-300">
          <!-- Pink Gradient Background -->
          <div class="absolute inset-0 bg-gradient-to-br from-pink-500 via-purple-600 to-purple-700"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 mx-auto mb-6 bg-white/20 rounded-2xl flex items-center justify-center backdrop-blur-sm">
              <LucideIcon name="heart" size={40} class="text-white" />
            </div>
            <h3 class="text-3xl font-bold mb-2">Test EQ</h3>
            <p class="text-pink-100 text-lg mb-4">Emotional Intelligence</p>
            <div class="flex items-center justify-center text-pink-100">
              <LucideIcon name="clock" size={16} class="mr-2" />
              30 minutes • 10 questions
            </div>
            <a href="/about" class="inline-block mt-6 px-8 py-3 bg-white text-purple-600 rounded-xl font-semibold hover:bg-purple-50 transition-colors">
              Sắp ra mắt
            </a>
          </div>
        </div>
      </div>

      <!-- Stats Section -->
      <div class="grid grid-cols-3 gap-8 text-center">
        <div>
          <div class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">2.5M+</div>
          <div class="text-gray-600 font-medium">Người dùng</div>
        </div>
        <div>
          <div class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">99.7%</div>
          <div class="text-gray-600 font-medium">Độ chính xác</div>
        </div>
        <div>
          <div class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">150+</div>
          <div class="text-gray-600 font-medium">Quốc gia</div>
        </div>
      </div>
      
    </div>
  </section>
</BaseLayout>

<style>
  /* Optimized minimal styles for better performance */
  @media (max-width: 768px) {
    .grid-cols-4 {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>