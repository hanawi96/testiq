---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getLocale } from '../utils/i18n';

const locale = getLocale('vi');
---

<BaseLayout
  title={locale.meta.site_name}
  description={locale.meta.description}
  keywords={locale.meta.keywords}
>
  <!-- Hero Section -->
  <section class="relative overflow-hidden bg-gradient-to-br from-primary-50 via-white to-blue-50 pt-20 pb-32">
    <!-- Background decoration -->
    <div class="absolute inset-0 overflow-hidden">
      <div class="absolute -top-40 -right-32 w-80 h-80 bg-primary-100 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse-slow"></div>
      <div class="absolute -bottom-40 -left-32 w-80 h-80 bg-blue-100 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse-slow" style="animation-delay: 1s;"></div>
    </div>
    
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <!-- Badge -->
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-primary-100 text-primary-800 text-sm font-medium mb-8 animate-bounce-in">
          <span class="mr-2">🧠</span>
          Test IQ Chuẩn Quốc Tế
        </div>
        
        <!-- Main heading -->
        <h1 class="text-4xl md:text-6xl font-display font-bold text-gray-900 mb-6 animate-fade-in">
          <span class="bg-gradient-to-r from-primary-600 via-blue-600 to-cyan-600 bg-clip-text text-transparent">
            {locale.home.title}
          </span>
        </h1>
        
        <!-- Subtitle -->
        <p class="text-xl md:text-2xl text-gray-600 font-medium mb-4 animate-slide-up">
          {locale.home.subtitle}
        </p>
        
        <!-- Description -->
        <p class="text-lg text-gray-700 max-w-3xl mx-auto mb-12 leading-relaxed animate-slide-up" style="animation-delay: 0.2s;">
          {locale.home.description}
        </p>
        
        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-slide-up" style="animation-delay: 0.4s;">
          <a 
            href="/about" 
            class="group inline-flex items-center px-8 py-4 text-lg font-semibold text-white bg-gradient-to-r from-primary-600 to-blue-600 rounded-xl shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300"
          >
            <span>{locale.home.cta}</span>
            <svg class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
          
          <a 
            href="/about" 
            class="inline-flex items-center px-8 py-4 text-lg font-semibold text-primary-600 bg-white border-2 border-primary-200 rounded-xl hover:bg-primary-50 hover:border-primary-300 transition-all duration-300"
          >
            Tìm hiểu thêm
          </a>
        </div>
        
        <!-- Trust indicators -->
        <div class="mt-16 animate-fade-in" style="animation-delay: 0.6s;">
          <p class="text-sm text-gray-500 mb-6">Được tin tưởng bởi hơn 500,000 người dùng</p>
          <div class="flex justify-center items-center space-x-12 opacity-60">
            <div class="text-2xl">⭐⭐⭐⭐⭐</div>
            <div class="text-sm text-gray-600">4.9/5 đánh giá</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section id="features" class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Section header -->
      <div class="text-center mb-20">
        <h2 class="text-3xl md:text-4xl font-display font-bold text-gray-900 mb-4">
          Tại sao chọn chúng tôi?
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Những tính năng vượt trội giúp bạn có trải nghiệm test IQ tốt nhất
        </p>
      </div>
      
      <!-- Features grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🎯</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Chính xác cao</h3>
          <p class="text-gray-600 leading-relaxed">Thuật toán đánh giá dựa trên tiêu chuẩn quốc tế</p>
        </div>
        
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">⚡</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Siêu nhanh</h3>
          <p class="text-gray-600 leading-relaxed">Giao diện tối ưu, tải nhanh trên mọi thiết bị</p>
        </div>
        
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🆓</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Hoàn toàn miễn phí</h3>
          <p class="text-gray-600 leading-relaxed">Không phí ẩn, không cần đăng ký</p>
        </div>
        
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">📊</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Kết quả chi tiết</h3>
          <p class="text-gray-600 leading-relaxed">Phân tích sâu với lời giải thích cho từng câu</p>
        </div>
        
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🌍</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Đa ngôn ngữ</h3>
          <p class="text-gray-600 leading-relaxed">Hỗ trợ Tiếng Việt, English, Español</p>
        </div>
        
        <div class="group p-8 bg-gray-50 rounded-2xl hover:bg-white hover:shadow-xl transition-all duration-300 border border-transparent hover:border-primary-100">
          <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">✨</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Giao diện hiện đại</h3>
          <p class="text-gray-600 leading-relaxed">Thiết kế đẹp mắt, trải nghiệm mượt mà</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-20 bg-gradient-to-r from-primary-600 to-blue-600">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-display font-bold text-white mb-4">
          Thống kê ấn tượng
        </h2>
      </div>
      
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="text-center">
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">500K+</div>
          <div class="text-primary-100 font-medium">Tests đã thực hiện</div>
        </div>
        
        <div class="text-center">
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">110</div>
          <div class="text-primary-100 font-medium">Điểm trung bình</div>
        </div>
        
        <div class="text-center">
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">50+</div>
          <div class="text-primary-100 font-medium">Quốc gia sử dụng</div>
        </div>
        
        <div class="text-center">
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">98%</div>
          <div class="text-primary-100 font-medium">Hài lòng</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-24 bg-gray-50">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <div class="bg-white rounded-3xl shadow-xl p-12 border border-gray-100">
        <div class="text-6xl mb-6">🎯</div>
        <h2 class="text-3xl md:text-4xl font-display font-bold text-gray-900 mb-6">
          Sẵn sàng khám phá tiềm năng của bạn?
        </h2>
        <p class="text-xl text-gray-600 mb-8 max-w-2xl mx-auto">
          Chỉ cần 55 phút để có được phân tích chi tiết về chỉ số thông minh và khả năng tư duy của bạn.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/about" 
            class="inline-flex items-center px-8 py-4 text-lg font-semibold text-white bg-gradient-to-r from-primary-600 to-blue-600 rounded-xl shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300"
          >
            <span class="mr-2">🚀</span>
            Bắt Đầu Test Ngay
          </a>
          
          <a 
            href="/about" 
            class="inline-flex items-center px-8 py-4 text-lg font-semibold text-gray-700 bg-gray-100 rounded-xl hover:bg-gray-200 transition-colors duration-300"
          >
            <span class="mr-2">📖</span>
            Tìm hiểu thêm
          </a>
        </div>
        
        <!-- Features highlight -->
        <div class="mt-12 flex flex-wrap justify-center gap-6 text-sm text-gray-600">
          <div class="flex items-center">
            <span class="mr-2">✅</span>
            Miễn phí hoàn toàn
          </div>
          <div class="flex items-center">
            <span class="mr-2">⚡</span>
            Kết quả ngay lập tức
          </div>
          <div class="flex items-center">
            <span class="mr-2">🔒</span>
            Bảo mật tuyệt đối
          </div>
          <div class="flex items-center">
            <span class="mr-2">🌍</span>
            Chuẩn quốc tế
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Additional animations */
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>

<script>
  // Add smooth scrolling for anchor links
  document.addEventListener('DOMContentLoaded', function() {
    const links = document.querySelectorAll('a[href^="#"]');
    
    links.forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();
        
        const targetId = this.getAttribute('href').substring(1);
        const targetElement = document.getElementById(targetId);
        
        if (targetElement) {
          targetElement.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
  });
  
  // Add intersection observer for animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate-fade-in');
      }
    });
  }, observerOptions);
  
  // Observe elements for animations
  document.querySelectorAll('.grid > div, section').forEach(el => {
    observer.observe(el);
  });
</script>