# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Public keys (safe to expose in client-side code)
PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Private keys (server-side only, never expose in client code)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Instructions:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > API
# 3. Copy the Project URL to PUBLIC_SUPABASE_URL
# 4. Copy the anon/public key to PUBLIC_SUPABASE_ANON_KEY  
# 5. Copy the service_role key to SUPABASE_SERVICE_ROLE_KEY
# 6. Save this file as .env (remove .example extension)
#
# WARNING: Never commit the .env file to version control!
# The service_role key has admin privileges and should be kept secret.
